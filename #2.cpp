#include <iostream>
#include <cmath>
using namespace std;

int main(){

    double x=2, y=2, z=3;

    x=x+((-y+(4*x))/(2*z))-(((x*(sqrt(y)))+6)/4);
    cout<<"the value of x is "<<x<<endl;

    return 0;
}
